@model DIRECTION_GESTOR.Models.Ciudades

@{
    ViewData["Title"] = "Create";
}
<br><br>
<h2>Agregar Ciudad</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="IdCiudad" class="control-label">Codigo </label>
                <input asp-for="IdCiudad" class="form-control" />
                <span asp-validation-for="IdCiudad" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DescripcionCiudad" class="control-label">Ciudad</label>
                <input asp-for="DescripcionCiudad" class="form-control" />
                <span asp-validation-for="DescripcionCiudad" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdPaisFk" class="control-label">Pais</label>
                <select asp-for="IdPaisFk" class="form-control" asp-items="ViewBag.IdPaisFk"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdEstadoFk" class="control-label">Estado</label>
                <select asp-for="IdEstadoFk" class="form-control" asp-items="ViewBag.IdEstadoFk"></select>
            </div>

            <div class="form-group">
                <input type="submit" value="Agregar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver Atras</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
