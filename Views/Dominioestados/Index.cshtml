@model IEnumerable<DIRECTION_GESTOR.Models.Dominioestados>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionDominioEstado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionDominioEstado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDominioEstado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDominioEstado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDominioEstado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
