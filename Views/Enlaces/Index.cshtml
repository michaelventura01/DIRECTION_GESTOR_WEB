@model IEnumerable<DIRECTION_GESTOR.Models.Enlaces>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionEnlace)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UrlEnlace)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEstadoFkNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdInstitucionFkNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdRolFkNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionEnlace)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UrlEnlace)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEstadoFkNavigation.DescripcionEstado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdInstitucionFkNavigation.DescripcionInstitucion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdRolFkNavigation.DescripcionRol)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEnlace">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEnlace">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEnlace">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
