@model IEnumerable<DIRECTION_GESTOR.Models.Institucionescorreos>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCorreoFkNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEstadoFkNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdInstitucionFkNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCorreoFkNavigation.DescripcionCorreo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEstadoFkNavigation.DescripcionEstado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdInstitucionFkNavigation.DescripcionInstitucion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdInstitucionCorreo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdInstitucionCorreo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdInstitucionCorreo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
