@model DIRECTION_GESTOR.Models.Sexos

@{
    ViewData["Title"] = "Create";
}
<br><br>
<h2>Agregar Sexo</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="DescripcionSexo" class="control-label">Sexo</label>
                <input asp-for="DescripcionSexo" class="form-control" />
                <span asp-validation-for="DescripcionSexo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Agregar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver Atras</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
