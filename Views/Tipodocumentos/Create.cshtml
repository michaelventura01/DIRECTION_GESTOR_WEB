@model DIRECTION_GESTOR.Models.Tipodocumentos

@{
    ViewData["Title"] = "Create";
}
<br><br>
<h2>Agregar Tipo de Documento</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="DescripcionTipoDocumento" class="control-label">Tipo de Documento</label>
                <input asp-for="DescripcionTipoDocumento" class="form-control" />
                <span asp-validation-for="DescripcionTipoDocumento" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdEstadoFk" class="control-label">Estado</label>
                <select asp-for="IdEstadoFk" class ="form-control" asp-items="ViewBag.IdEstadoFk"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Agregar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver Atras</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
