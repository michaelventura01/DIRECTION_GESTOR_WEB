@model IEnumerable<DIRECTION_GESTOR.Models.Tiposrelaciones>

@{
    ViewData["Title"] = "Index";
}
<br><br>
<h2>Relaciones de Personas</h2>
<br>
<p>
    <a asp-action="Create" class="btn btn-primary">Agregar Relacion</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Descripcion
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionTipoRelacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoRelacion"><i class="glyphicon glyphicon-edit"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoRelacion"><i class="glyphicon glyphicon-book"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoRelacion"><i class="glyphicon glyphicon-remove"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
